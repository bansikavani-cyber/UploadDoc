// kt: This folder contains Bicep templates for provisioning Azure resources for each environment (Dev, QA, Prod).
// kt: Each file (dev.bicep, qa.bicep, prod.bicep) will define Storage Account, App Service, and Database resources.
